{
  "name": "htmsh",
  "version": "0.1.8",
  "description": "Turn your code into a live website instantly from your shell. No setup, no servers, no stress. Just one command and your HTML, CSS, and JavaScript are live for the world to see.",
  "type": "module",
  "bin": {
    "htmsh": "./dist/htm.modern.js"
  },
  "scripts": {
    "build": "microbundle build --entry cli/htm.js --output dist/htm.js --format modern --target node --compress --no-sourcemap",
    "postbuild": "chmod +x dist/htm.modern.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "conf": "^13.0.1",
    "inquirer": "^9.3.7",
    "kolorist": "^1.8.0",
    "tar": "^6.2.0"
  },
  "files": [
    "dist/",
    "lib/",
    "README.MD"
  ],
  "keywords": [
    "deploy",
    "static",
    "cli",
    "spa",
    "surge"
  ],
  "license": "ISC",
  "devDependencies": {
    "microbundle": "^0.15.1"
  }
}
